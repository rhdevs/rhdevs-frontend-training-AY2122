import styled from 'styled-components'

export const StyledHeader = styled.h1`
  text-align: center;
  margin: 0;
  font-size: calc(24px + 0.2vw);
`

export const ActionButtonsContainer = styled.div`
  width: 8rem;
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  align-items: center;
`

export const StyledTable = { margin: 'auto' }

export const MainContainer = styled.div`
  width: 80vw;
  max-width: 850px;
  margin: auto;
`

export const TitleAndButtonSection = styled.div`
  display: flex;
  flex-direction: row;
  justify-content: space-between;
  margin: 25px auto;
  align-items: center;
`

export const StyledAddButton = { width: '120px' }
